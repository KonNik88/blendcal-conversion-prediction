# Copy to .env and adjust as needed; docker-compose will auto-load it.
# This file is mainly for DEV runs (docker compose up --build).
# Release compose may set these inline or images may have defaults baked in.

########################
# API service
########################

# Where API expects artifacts inside the container
FINAL_PROJECT_ROOT=/app

# Optional: log level for API (if supported by app logger)
LOG_LEVEL=INFO

# Optional: container timezone (helps with log timestamps)
TZ=Europe/Berlin

# Note: MODEL_VERSION here is only a label (if your code reads it).
# The /version endpoint in release takes version from project artifacts/files.
# You can keep or remove this; it does NOT affect model behavior.
MODEL_VERSION=dev

########################
# UI (Streamlit) service
########################

# API endpoint visible from UI inside the Docker network.
# Keep http://api:8000 (service name "api" from docker-compose).
BLENDCAL_API=http://api:8000

# Optional: if you ever expose the UI on a different port via compose vars.
# UI_PORT=8501

########################
# General notes
########################
# - Commit this file as env.example; DO NOT commit your local .env.
# - For release: docker-compose.release.yml may define env inline; .env is not required.
# - For dev: create .env next to docker-compose.yml by copying this file.
